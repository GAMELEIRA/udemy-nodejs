let soma = (a, b) => a + b;

let multiplicacao = (a, b) => a * b;

let divisao = (a, b) => a / b;

let subtracao = (a, b) => a - b;

module.exports = {

    soma,
    multiplicacao,
    divisao,
    subtracao

};